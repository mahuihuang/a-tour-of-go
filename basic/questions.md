# 问题

## 如何定义包名？

## fmt.Println() 第一个参数的末尾是否需要保留空格？

## 如何格式化输出变量？

## 如何判断包内的定义的函数，结构等能否被引用？

## 如何定义函数？主函数和普通函数

## 当函数的相邻参数是相同类型，应该如定义函数参数？

## 单个返回参数与多个返回参数的写法差异？可参考引包写法

## 函数返回参数定义了变量名称有何作用，例如 `func sum() (x, y int)` ？不推荐使用

## 如何定义变量？基础变量的默认值是什么？

## 根据根据函数作用域范围可分为哪两类变量？

## 如何是一个 var 关键词定义多个变量？

## 如何定义代初始值的变量？

## `:=` 简短变量表达式有什么作用？使用的时候需要注意什么？
